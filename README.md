# Docs for stsstack-bundles project

This is a mirror of the docs from https://launchpad.net/stsstack-bundles so that we can easily test the markdown docs (since Launchpad doesn't support doing that).
